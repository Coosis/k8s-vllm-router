apiVersion: v1
kind: Service
metadata:
  name: vllm-service
spec:
  type: NodePort
  selector:
    app: vllm
  ports:
    - protocol: TCP
      port: 8976
      targetPort: 8000
      nodePort: 30976
